PORT=5001
MONGODB_URI=mongodb://127.0.0.1:27017/the-hot-chick
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRE=7d

# Optional: only needed if payment routes create Stripe checkout/payment intents.
STRIPE_SECRET_KEY=
